#include <bits/stdc++.h>
#define REPS(i, a, n) for (long long i = (a); i < (long long)(n); i++)
#define REP(i, n) for (int i = 0; i < (int)n; i++)
#define YesNo(bool)            \
    if (bool) {                \
        cout << "Yes" << endl; \
    } else {                   \
        cout << "No" << endl;  \
    }
#define YESNO(bool)            \
    if (bool) {                \
        cout << "YES" << endl; \
    } else {                   \
        cout << "NO" << endl;  \
    }
using namespace std;

int main(void) {
    // code here

    return 0;
}